import { SecretType } from '../../SecretType';
import { Network } from '../../Network';
import { BuildTransferRequestBase } from './BuildTransferRequestBase';
import { BuildTokenTransferRequestDto } from './BuildTokenTransferRequestDto';
export declare class BuildTokenTransferRequest extends BuildTransferRequestBase implements BuildTokenTransferRequestDto {
    value: number;
    tokenAddress: string;
    static fromData(requestData: BuildTokenTransferRequestDto): BuildTokenTransferRequest;
    constructor(walletId: string, to: string, value: number, secretType: SecretType, tokenAddress: string, network?: Network, chainSpecificFields?: string);
}
