import { TransactionRequest } from '../../TransactionRequest';
export interface VechainTransactionRequestClause {
    to: string;
    data: string;
    amount: number;
}
export declare class VechainTransactionRequest extends TransactionRequest {
    blockRef?: string;
    chainTag?: string;
    expiration: number;
    gas: number;
    gasPriceCoef: number;
    nonce?: string;
    clauses: VechainTransactionRequestClause[];
}
