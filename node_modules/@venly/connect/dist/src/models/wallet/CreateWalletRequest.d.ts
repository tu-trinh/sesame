import { SecretType } from '../SecretType';
import { ConfirmationRequest } from '../ConfirmationRequest';
import { ConfirmationRequestType } from '../ConfirmationRequestType';
export declare abstract class CreateWalletRequest implements ConfirmationRequest {
    abstract readonly confirmationRequestType: ConfirmationRequestType;
    readonly secretType: SecretType;
    description?: string;
    constructor(secretType: SecretType);
}
