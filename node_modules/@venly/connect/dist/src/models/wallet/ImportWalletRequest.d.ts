import { ConfirmationRequestType } from '../ConfirmationRequestType';
import { ConfirmationRequest } from '../ConfirmationRequest';
import { SecretType } from '../SecretType';
export declare class ImportWalletRequest implements ConfirmationRequest {
    walletId: string;
    to: SecretType;
    readonly confirmationRequestType: ConfirmationRequestType;
    static fromData(data: ImportWalletRequest): ImportWalletRequest;
    constructor(walletId: string, to: SecretType);
}
